export interface User {
    id: string; // Unique identifier for the user
    name: string; // Name of the user
    email: string; // Email address of the user
    // Add more properties as needed, e.g., role, avatar, etc.
    // role?: string;
  }